/** @format */

import React from "react";

const ImageBox = () => {
  return (
    <div>
      <div>앨범 이미지</div>
      {/* {album && <img alt={} src={} />} */}
    </div>
  );
};

export default ImageBox;
